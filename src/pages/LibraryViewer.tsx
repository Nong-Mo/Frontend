import React, { useState } from "react";
import { NavBar } from "../components/common/NavBar.tsx";

const LibraryViewer = () => {
    return (
        <div>
            <h1>Library Viewer Page</h1>
        </div>
    );
};

export default LibraryViewer;